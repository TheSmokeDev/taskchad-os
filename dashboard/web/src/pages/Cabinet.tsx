import { Placeholder } from './Placeholder';

/**
 * Cabinet page — donor's WarRoom.tsx renamed per Q-naming lock
 * (PRP-prd-8-phase-3-dashboard-port.md R7 ADOPT-WITH-FIXES). Multi-
 * persona voice room with operator-as-participant ships in Phase 5+.
 */
export function Cabinet() {
  return (
    <Placeholder
      title="Cabinet"
      description="Multi-persona voice room with operator-as-participant ships in a later phase. The framework name is Cabinet (donor called this WarRoom)."
    />
  );
}
